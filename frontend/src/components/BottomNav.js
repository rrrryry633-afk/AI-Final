// This component is now deprecated. 
// Use PortalLayout component instead which includes the bottom navigation.
// Keeping this file for backwards compatibility - it re-exports from PortalLayout.

import { BottomNav } from './PortalLayout';
export default BottomNav;
